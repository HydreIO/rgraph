language: node_js

os: linux

node_js:
  - node

before_deploy:
  - echo "Deploying to npm ..."
  - rm -rf src
  - mv src/* ./

deploy:
  provider: npm
  edge: true
  email: '$NPM_EMAIL'
  api_token: '$NPM_TOKEN'
  cleanup: false
  on:
    tags: true
    branch: master
